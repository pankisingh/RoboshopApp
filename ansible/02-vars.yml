- name: Demo on variables
  hosts: all
  vars:
    URL: google.com
  tasks:
    - name: Print variable
      ansible.builtin.debug:
        msg: "URL= {{URL}}"
    - name: Print task variable    
      ansible.builtin.debug:
        msg: "URL={{URL}}"
      vars:
        URL: taks.google.com



